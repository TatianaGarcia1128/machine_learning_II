from setuptools import setup, find_packages

setup(
    name='unsupervised_package',
    version='0.1',
    packages=find_packages(),
    install_requires=[],  # Lista de dependencias si las tienes
    # Metadatos adicionales como autor, descripción, etc.
)
